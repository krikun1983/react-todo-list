import { createTodoListItem } from '../utils';

const todoData = [
  createTodoListItem('Learn React'),
  createTodoListItem('Make Awesome App'),
  createTodoListItem('Drink coffee'),
];

export default todoData;
